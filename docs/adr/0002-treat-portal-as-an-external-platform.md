# Treat Portal as an external platform

Portal Messenger is built as an ordinary Portal customer using only hosted APIs and published `@portalsdk/*` packages. Contributors and forkers never need access to the private Portal repository, and application requirements must not depend on modifying or locally running Portal infrastructure.
